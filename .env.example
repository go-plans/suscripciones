# Ejemplo para el entorno local — copiar a `.env` y rellenar desde API KEYS.txt
# ⚠️ Nunca versionar `.env` (ya está en .gitignore). Solo VITE_* llega al bundle,
#    y solo variables PÚBLICAS: la service_role NO puede usarse en el frontend.

VITE_SUPABASE_URL=https://xbmewcmpfnligeodggop.supabase.co
VITE_SUPABASE_ANON_KEY=paste-aqui-la-anon-key-publica